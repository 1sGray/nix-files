-- https://wiki.hypr.land/Configuring/Basics/Monitors/
-- Run `hyprctl monitors` once logged in, then replace with an explicit
-- mode/position, same as you'd fill in niri's outputs.kdl.
hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = "auto",
})
